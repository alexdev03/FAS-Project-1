[log]
# regex (case insensitive) used to filter system log entries
regex = error|failed|critical|oom|killed

# how many recent log lines to scan each cycle
lines = 500

[git]
repo = utente/nome-repo

# seconds between each collection cycle
intervallo = 600
